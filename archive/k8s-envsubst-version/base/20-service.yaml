apiVersion: v1
kind: Service
metadata:
  name: user1-web
  namespace: user1
  labels:
    app: user1-web
    tenant: user1
spec:
  type: ClusterIP
  selector:
    app: user1-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
